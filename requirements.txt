# Dependência da função Serverless Python da Vercel (api/extrair_anexo.py).
# pdfplumber (pdfminer.six + Pillow) faz a extração de tabelas do PDF do edital.
pdfplumber==0.11.4

# --- OCR para PDFs escaneados (só imagem, sem camada de texto) ---
# Usadas APENAS no fallback OCR: rasteriza as páginas (pypdfium2) e reconhece o
# texto (rapidocr-onnxruntime, pip-only, SEM binário de sistema como tesseract).
# São pesadas (onnxruntime + opencv + modelos ~150MB) e o OCR leva ~5s/página em
# CPU. Recomendado principalmente para o extrator LOCAL (scripts/extrair_anexo_
# local.py). Na Vercel, atente ao limite de tamanho da função e ao timeout; se
# preferir não embarcá-las lá, o extrator continua funcionando para PDFs com
# texto e apenas informa que o OCR está indisponível para escaneados.
pypdfium2==5.7.1
rapidocr-onnxruntime==1.2.3
numpy>=1.26,<3
